spring.application.name=SearchTest

# dummy Kafka config (da ne puca contextLoad)
spring.kafka.bootstrap-servers=localhost:9999
spring.kafka.consumer.group-id=test-group

# dummy Keycloak config (da ne ide na pravi server)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://dummy
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://dummy/certs

# CORS dummy
app.cors.allowed-origins=http://localhost:5173
logging.level.org.springframework.data.elasticsearch.client.WIRE= TRACE
logging.level.org.elasticsearch.client= DEBUG
